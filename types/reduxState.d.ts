type ScreenRecordType = {
  aspectRatio: string;
  frameRate: string;
  resolutions: string;
};

type PomodoroTechniqueType = {
  settings: string[];
  workTime: number;
  restTime: number;
  musicType: string;
};
